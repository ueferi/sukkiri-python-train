# スライスによる範囲指定
a = [10, 20, 30, 40, 50]
print(a[1:3])  # 添え字が1以上3未満の要素
print(a[2:])  # 添え字が2以上の全ての要素
print(a[:3])  # 添え字が3未満の要素

# 負の数による指定
a = [10, 20, 30, 40, 50]
print(a[-1])  # 末尾の要素を参照
print(a[-2])  # 末尾から2番目の要素を参照
